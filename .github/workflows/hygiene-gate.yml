name: hygiene-gate
on:
  push:
    branches: ["**"]
  pull_request:
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - name: Run hygiene scan
        run: bash .hygiene/preflight-scan.sh
